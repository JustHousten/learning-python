import random

# Zufällige ganze Zahl zwischen 1 und 10
zahl = random.randint(1, 10)
print(zahl)

# Zufälliges Element aus einer Liste
aktionen = ["Normaler Angriff", "Kritischer Treffer", "Verfehlt"]
aktion = random.choice(aktionen)
print(aktion)